.media_collection
  .widget
    .collection_title
      h3= media_collection.get('name')
    .progress_container
      #progress_bar.collection_progress
  .existing_media_items
    != console.log(JSON.stringify(media_collection))
    != partial('media_item', media_collection.get('items'))
